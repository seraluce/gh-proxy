{
  "name": "proxy-hub",
  "version": "1.0.0",
  "description": "Cloudflare Workers 代理工具集",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
